---
interface Props {
  emoji: string;
  title: string;
  description: string;
  linkText?: string;
}

const { 
  emoji, 
  title, 
  description, 
  linkText = "Get started →" 
} = Astro.props;
---

<div class="group bg-white rounded-2xl p-8 shadow-sm hover:shadow-xl transition-all duration-300 cursor-pointer border border-slate-100 hover:border-indigo-200">
  <div class="text-5xl mb-4">{emoji}</div>
  <h3 class="text-2xl font-bold text-slate-900 mb-3">{title}</h3>
  <p class="text-slate-600 mb-4">{description}</p>
  <span class="text-indigo-600 font-medium group-hover:underline">{linkText}</span>
</div>
