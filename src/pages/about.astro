---
import Layout from '../layouts/Layout.astro';
import { siteConfig } from '../data/siteConfig';
import { generateSEOProps } from '../utils/seo';

const seoProps = generateSEOProps({
  title: 'About Us - Civic Portal',
  description: 'Learn about our mission to make civic services accessible to everyone.'
});
---

<Layout title={seoProps.title || ''}>
  <main class="min-h-screen bg-linear-to-br from-slate-50 via-blue-50 to-indigo-50">
    <div class="max-w-4xl mx-auto px-6 py-16">
      
      <!-- Header -->
      <div class="text-center mb-16">
        <div class="text-6xl mb-6">🏛️</div>
        <h1 class="text-4xl md:text-5xl font-bold text-slate-900 mb-6">About Civic Portal</h1>
        <p class="text-xl text-slate-600 max-w-3xl mx-auto">
          We're on a mission to make civic services simple, accessible, and user-friendly for everyone.
        </p>
      </div>

      <!-- Mission & Vision -->
      <section class="grid md:grid-cols-2 gap-8 mb-16">
        <div class="bg-white rounded-2xl p-8 shadow-sm border border-slate-100">
          <h2 class="text-2xl font-bold text-slate-900 mb-4">Our Mission</h2>
          <p class="text-slate-600 leading-relaxed">
            To democratize access to civic services by providing clear, straightforward guidance that helps people navigate life's most important moments with confidence and ease.
          </p>
        </div>
        <div class="bg-white rounded-2xl p-8 shadow-sm border border-slate-100">
          <h2 class="text-2xl font-bold text-slate-900 mb-4">Our Vision</h2>
          <p class="text-slate-600 leading-relaxed">
            A world where every citizen can easily access and understand government services, regardless of their background, technical skills, or life circumstances.
          </p>
        </div>
      </section>

      <!-- Stats -->
      <section class="bg-linear-to-r from-indigo-600 to-blue-600 rounded-3xl p-12 text-center text-white mb-16">
        <h2 class="text-3xl font-bold mb-8">Making a Difference</h2>
        <div class="grid grid-cols-2 md:grid-cols-4 gap-8">
          <div>
            <div class="text-4xl font-bold mb-2">250K+</div>
            <div class="text-indigo-200">People Helped</div>
          </div>
          <div>
            <div class="text-4xl font-bold mb-2">4.8★</div>
            <div class="text-indigo-200">Average Rating</div>
          </div>
          <div>
            <div class="text-4xl font-bold mb-2">24/7</div>
            <div class="text-indigo-200">Support Available</div>
          </div>
          <div>
            <div class="text-4xl font-bold mb-2">99%</div>
            <div class="text-indigo-200">Satisfaction Rate</div>
          </div>
        </div>
      </section>

      <!-- Values -->
      <section class="mb-16">
        <h2 class="text-3xl font-bold text-slate-900 text-center mb-12">Our Values</h2>
        <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
          {[
            {
              icon: '🤝',
              title: 'Accessibility',
              description: 'Services should be accessible to everyone, regardless of background or ability.'
            },
            {
              icon: '🎯',
              title: 'Clarity',
              description: 'Complex processes should be explained in simple, understandable terms.'
            },
            {
              icon: '⚡',
              title: 'Efficiency',
              description: 'Save time and reduce frustration with streamlined processes.'
            },
            {
              icon: '🛡️',
              title: 'Trust',
              description: 'Build confidence through reliable, accurate information.'
            },
            {
              icon: '🌱',
              title: 'Growth',
              description: 'Continuously improve based on user feedback and needs.'
            },
            {
              icon: '🤗',
              title: 'Empathy',
              description: 'Understand and address the real challenges people face.'
            }
          ].map((value) => (
            <div class="bg-white rounded-xl p-6 border border-slate-100 text-center">
              <div class="text-4xl mb-4">{value.icon}</div>
              <h3 class="font-semibold text-slate-900 mb-2">{value.title}</h3>
              <p class="text-slate-600 text-sm">{value.description}</p>
            </div>
          ))}
        </div>
      </section>

      <!-- Team -->
      <section class="mb-16">
        <h2 class="text-3xl font-bold text-slate-900 text-center mb-12">Our Team</h2>
        <div class="grid md:grid-cols-3 gap-8">
          {[
            {
              name: 'Sarah Johnson',
              role: 'Executive Director',
              bio: 'Former government official with 15+ years of public service experience.'
            },
            {
              name: 'Michael Chen',
              role: 'Head of User Experience',
              bio: 'Passionate about making complex systems simple and accessible.'
            },
            {
              name: 'Emily Rodriguez',
              role: 'Community Outreach Manager',
              bio: 'Dedicated to ensuring all voices are heard and served.'
            }
          ].map((member) => (
            <div class="bg-white rounded-xl p-6 border border-slate-100 text-center">
              <div class="w-20 h-20 bg-indigo-100 text-indigo-600 rounded-full flex items-center justify-center mx-auto mb-4 text-2xl font-bold">
                {member.name.split(' ').map(n => n[0]).join('')}
              </div>
              <h3 class="font-semibold text-slate-900 mb-1">{member.name}</h3>
              <p class="text-indigo-600 text-sm font-medium mb-3">{member.role}</p>
              <p class="text-slate-600 text-sm">{member.bio}</p>
            </div>
          ))}
        </div>
      </section>

      <!-- Timeline -->
      <section class="mb-16">
        <h2 class="text-3xl font-bold text-slate-900 text-center mb-12">Our Journey</h2>
        <div class="relative">
          <div class="absolute left-8 top-0 bottom-0 w-0.5 bg-indigo-200"></div>
          {[
            { year: '2020', event: 'Founded with a mission to simplify civic services' },
            { year: '2021', event: 'Launched first version serving 10,000 users' },
            { year: '2022', event: 'Expanded to cover all major life events' },
            { year: '2023', event: 'Reached 100,000 people helped milestone' },
            { year: '2024', event: 'Enhanced platform with AI-powered assistance' }
          ].map((milestone, index) => (
            <div class="relative flex items-center mb-8">
              <div class="w-16 h-16 bg-indigo-600 text-white rounded-full flex items-center justify-center font-bold z-10">
                {milestone.year}
              </div>
              <div class="ml-6 bg-white rounded-xl p-4 border border-slate-100 flex-1">
                <p class="text-slate-700">{milestone.event}</p>
              </div>
            </div>
          ))}
        </div>
      </section>

      <!-- Partners -->
      <section class="bg-white rounded-2xl p-8 shadow-sm border border-slate-100">
        <h2 class="text-2xl font-bold text-slate-900 text-center mb-8">Our Partners</h2>
        <div class="grid grid-cols-2 md:grid-cols-4 gap-8 items-center">
          {['Department of Health', 'City Hall', 'County Clerk', 'Social Security', 'DMV', 'Courts System', 'Housing Authority', 'Business Bureau'].map((partner) => (
            <div class="text-center">
              <div class="w-16 h-16 bg-slate-100 rounded-lg mx-auto mb-2 flex items-center justify-center">
                <span class="text-2xl">🏛️</span>
              </div>
              <p class="text-sm text-slate-600">{partner}</p>
            </div>
          ))}
        </div>
      </section>

    </div>
  </main>
</Layout>
